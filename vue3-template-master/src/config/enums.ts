export enum ECacheKey {
  TOKEN = 'token',
  ACCOUNT = 'account',
  SIDEBAR_STATUS = 'sidebar_status',
  SETTING = 'setting'
}

export enum EThemeType {
  LIGHT = 'light',
  DARK = 'dark'
}

export enum EMenuType {
  CATALOGUE = 'M',
  MENU = 'C',
  BUTTON = 'A'
}
