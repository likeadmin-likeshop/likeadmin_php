const config = {
    terminal: 1,
    title: '后台管理系统',
    version: '1.2.1',
    baseUrl: `${import.meta.env.VITE_APP_BASE_URL}/`,
    urlPrefix: 'adminapi',
    timeout: 10 * 1000
}

export default config
