/**
 * 权限
 */
 import { asyncRoutes } from '@/router'
const state = {
    sidebar: asyncRoutes[0].children
}

const mutations = {}

const actions = {}

export default {
    state,
    mutations,
    actions,
}
